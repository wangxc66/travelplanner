## 这个 PR 做了什么

<!-- 一两句话说清楚。评审的人不该需要读完 diff 才知道你在干嘛。 -->

Closes #

## 怎么验证

<!-- 评审的人照着这几步就能跑出你的结果。例如：
1. ./gradlew bootRun
2. curl -X POST localhost:8080/api/trips -H 'Content-Type: application/json' -d '{...}'
3. 应该返回 201 和新建的 trip
-->

## 自查

- [ ] `./gradlew build` 在本地能通过
- [ ] 没有提交密钥、`.env`、IDE 配置或 `build/`
- [ ] 改了接口的话，前端仓库已经开了对应的 issue
